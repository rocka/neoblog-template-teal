nav
    div(class="nav-list")
        each item in nav
            a(href=item.link) #{item.name}

div(class="header" style=`background-image: url(${assets('header.jpg')})`)
    div(class="motto")
        p(class="upper") Talk is cheap
        p(class="lower") Show me the code
