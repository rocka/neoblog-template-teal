extends layout

block append head
    if isso
        include isso

block content
    div(class="paper")
        article
            h1 #{article.meta.title}
            p(class="meta") #{formatDate(article.meta.date)}
            != article.html
    if isso
        #isso-thread.isso-thread
